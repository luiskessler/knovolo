import redisUser from "~/server/redis";

export const updateOrganizationInCache = async ({organizationID, organizationData} : {organizationID: string, organizationData: any}) => {
    
    await redisUser
    
};    